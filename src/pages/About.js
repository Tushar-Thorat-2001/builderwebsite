import React from "react";
import { Typography } from "@mui/material";
import { Box } from "@mui/system";

const About = () => {
  return (
    <Box textAlign="center">
      <Typography variant="h1">I am in About</Typography>
    </Box>
  );
};

export default About;
